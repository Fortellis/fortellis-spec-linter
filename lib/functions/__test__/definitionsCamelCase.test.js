const definitionsCamelCase = require("../definitionsCamelCase");

describe("definitionsCamelCase function", () => {
  test("should exist", () => {
    expect(definitionsCamelCase).toBeDefined();
    expect(typeof definitionsCamelCase).toBe("function");
  });
});
